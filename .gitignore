# OS / editors
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp

# Node (the hooks have no dependencies, but just in case)
node_modules/
*.log

# Internal handoff document: do not publish (private config and paths)
HANDOFF.md

# Cortex working store: this plugin's OWN repo must never commit a runtime .cortex/
# (it is seeded inside the *user's* project when the plugin runs, not here).
.cortex/
